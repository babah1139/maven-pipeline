pipeline {
  
  agent {
  label 'babah-agent'
     }

  tool {
    maven 'Maven'
  }

  stages{
    stage('Build Stage') {
      steps{
        sh 'mvn clean package'
      }
    }

    stage('code scan stage') {
        steps {
          sh '      '
         
        }
    }
  }







}